/** Host half of dsh-section-nav: the browser half owns all product behavior. */
export const name = "dsh-section-nav";

/** Cordis entry point; the rail is mounted by the browser half. */
export function apply(): void {}
